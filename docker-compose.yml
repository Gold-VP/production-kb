services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file: .env
    volumes:
      - ./kb:/app/kb
      - ./source-pdfs:/app/source-pdfs
      - ./logs:/app/logs
    restart: unless-stopped

  # influxdb:  # Раскомментировать в итерации 3
  #   image: influxdb:2.7
  #   ports:
  #     - "8086:8086"
  #   volumes:
  #     - influxdb-data:/var/lib/influxdb2
  #   restart: unless-stopped

# volumes:
#   influxdb-data:
